# Management Sources

## Private Root

Canonical path for all agent hosts. Sibling management skills read this first.

Path: ~/.agents/management/home-os

Brag-doc optional criteria file in the **same private root** as this file: `expectations.md` — see `brag-doc` skill; do not commit filled criteria to git.

## Meeting Notes

Primary system:
Shared index:
Per-direct pattern:

### Me (brag-doc / self)

Granola or meeting index links the agent should read for **your** wins in-window (optional):

## Linear

Workspace:
Team links:
Project links:
User lookup notes:

### Me (brag-doc / self)

Your Linear user id, handle, or “assigned to me” filter notes:

## GitHub

Organization:
Relevant repos:
PR search pattern:
Review/comment search pattern:
Fallback command notes:

### Me (brag-doc / self)

Your GitHub username and repo list or org scope for authored PRs and reviews:

## Slack

Workspace name (human-readable):
MCP server label in Cursor (if known, e.g. plugin-slack-slack):
Team or project channels (names or IDs, read scope only):
Default search hints (keywords, initiative names — optional):
Policy: DMs off by default unless listed; no tokens or secrets here — auth lives in MCP host config.

### Me (brag-doc / self)

Channels and thread URLs the agent may read for **your** activity (brag-doc never expands beyond this list unless you add DMs explicitly):

## Notion

MCP server label in Cursor (if known):

### Me (brag-doc / self)

Work log DBs/pages, weekly notes, or ship lists the agent should query. Brag-doc may also run a capped in-window “edited by me” search when the MCP supports it, in addition to these URLs.

## Hiring

Active role slug:
Interview rubric:
HR feedback template:

Hiring paths point to private filled rubrics under hiring/rubrics/. HR feedback template may stay on the git template path unless customized.

## Strategy

Planning docs:
Roadmap docs:
Product/org context:
