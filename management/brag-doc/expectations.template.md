# Job expectations (brag-doc criteria)

Private file at the Home OS root. Used only by `brag-doc`. Never commit filled content to git.

Paste role expectations, level rubric, success criteria, or a link the agent should fetch into prose below.

## Criteria

<!-- Bullet or numbered list. One criterion per line or short paragraph. -->

## Optional links

<!-- Notion / internal doc URLs for canonical rubric text. -->

To opt out of criteria mapping, do **not** edit this template in git. Ask `brag-doc` to decline — it writes private `expectations.md` containing exactly `## Status: declined`. See [`SKILL.md`](SKILL.md) (**Expectations gate**).
